// WARNING: All the keys are required unless otherwise specified.
{
  // Set this to true if you want to store critical info in a .env file (or enviroment variables).
  USE_ENV: false,
  // Basic bot config.
  BOT: {
    // The bot's token, optional if USE_ENV is true.
    TOKEN: '',
    // The webhook where erros will be sent, optional if USE_ENV is true.
    ERROR_WEBHOOK_URL: '',
    // The webhook where logs will be sent, optional if USE_ENV is true.
    LOG_WEBHOOK_URL: '',
    // Bot admin ids.
    ADMIN_IDS: [
      512640455834337290
    ],
    // The bot's version.
    VERSION: '0.0.1',
    // The bot's GitHub repo's url.
    GITHUB_REPO_URL: 'https://github.com/Clutter-Development/Clutter',
    // The bot's support server invite url.
    DISCORD_INVITE_URL: 'https://discord.com/invite/mVKkMZRPQE',
    // The bot's documentation url.
    DOCUMENTATION_URL: 'https://clutter-development.github.io/',
    // The bot's invite link.
    INVITE_URL: 'https://discord.com/oauth2/authorize?client_id=902405088138653726&permissions=8&scope=bot',
    // The bot's default prefix (for traditional commands).
    DEFAULT_PREFIX: '>',
    // The bot's default language.
    DEFAULT_LANGUAGE: 'en-US',
    // The bot development server IDs.
    DEVELOPMENT_SERVER_IDS: [944535258722861106],
    // Development mode, if true the bot will not accept any commands unless the user is a bot admin.
    DEVELOPMENT_MODE: false,
  },
  // Basic database config.
  DATABASE: {
    // The MongoDB connect URI. Optional if USE_ENV is true.
    URI: '',
    // The MongoDB database name.
    NAME: 'Clutter',
    // The cooldown for database caching.
    CACHE_COOLDOWN: 1200,
  },
  // Basic style config.
  STYLE: {
    COLORS: {
      SUCCESS: 0xFFFFFF,
      ERROR: 0xFFFFFF,
      WARNING: 0xFFFFFF,
      INFO: 0xFFFFFF
    },
    EMOJIS: {
      SUCCESS: '',
      ERROR: '',
      WARNING: '️',
      INFO: ''
    },
  }
}